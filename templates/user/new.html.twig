{% extends 'base.html.twig' %}

{% block title %}Nouvel utilisateur
{% endblock %}

{% block body %}

	<div class="blockPage loginPage">
		<div class="block">
			<h1 class="h3 mb-3 font-weight-normal">Créez votre compte</h1>
			{{ form_start(form) }}
			{{ form_widget(form) }}
			<div class="groupe-button-option">
				<a class="btn btn-sm" href="{{ path('app_login') }}" class="dropdown-item">Annuler</a>
				<button class="btn btn-sm">{{ button_label|default('Valider') }}</button>
			</div>
			{{ form_end(form) }}
		</div>
	</div>

{% endblock %}
